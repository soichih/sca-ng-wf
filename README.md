This component provides common SCA workflow UI components (angular directives)

* service requirement

appconf (with sca standard config)

* dependencies

ng-file-upload

## TODO

Maybe I should turn this into bower component and let each workflow install it via bower?
